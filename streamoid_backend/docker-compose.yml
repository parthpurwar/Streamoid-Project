version: "3.9"

services:
  django:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: streamoid_backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - DEBUG=1
    restart: always
